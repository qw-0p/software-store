// import { User, Basket, BasketProduct } from '.'

// User.hasOne(Basket, { foreignKey: 'userId' })
// Basket.belongsTo(User, { foreignKey: 'userId' })

// Basket.hasMany(BasketProduct)
// BasketProduct.belongsTo(Basket)
